{
  "name": "@easymotion/openapi",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "./dist/Api.js",
  "types": "./dist/Api.d.ts",
  "scripts": {
    "build": "tsc",
    "client": "swagger-typescript-api -p ./schema.json -o ./src",
    "generate": "pnpm client && pnpm build",
    "prepare": "pnpm generate",
    "clean": "rm -rf dist && rm -rf node_modules && rm -rf src"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "swagger-typescript-api": "^13.0.23"
  }
}
